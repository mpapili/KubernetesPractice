#! /bin/bash

echo "you can't! but you can create a deployment and those are very close in syntax"

echo "try: kubectl create deployment mikes-deployment --image=docker-registry:5000/fluentd --dry-run=client -o yaml > daemonset.yaml"
